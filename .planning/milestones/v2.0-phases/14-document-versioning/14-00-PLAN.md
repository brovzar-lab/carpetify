---
phase: 14-document-versioning
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/versioning/diffCompute.test.ts
  - src/__tests__/functions/documentStore.test.ts
  - src/__tests__/functions/revertDocument.test.ts
autonomous: true
requirements: [AIGEN-V2-03, AIGEN-V2-04, AIGEN-V2-05]
must_haves:
  truths:
    - "Test stubs exist for all 3 requirement areas before implementation begins"
    - "Spanish prose diff test case validates accented characters per D-07"
    - "documentStore test stubs cover version snapshot and prune logic"
    - "revertDocument test stubs cover copy-forward revert logic"
  artifacts:
    - path: "src/__tests__/versioning/diffCompute.test.ts"
      provides: "Diff computation tests with Spanish prose (D-07)"
      contains: "produccion"
    - path: "src/__tests__/functions/documentStore.test.ts"
      provides: "Version snapshot and prune test stubs"
      contains: "snapshots previous version"
    - path: "src/__tests__/functions/revertDocument.test.ts"
      provides: "Revert copy-forward test stubs"
      contains: "copy-forward"
  key_links:
    - from: "src/__tests__/versioning/diffCompute.test.ts"
      to: "diff"
      via: "import diffWords from diff library"
      pattern: "diffWords"
    - from: "src/__tests__/functions/documentStore.test.ts"
      to: "functions/src/pipeline/documentStore.ts"
      via: "import saveGeneratedDocument"
      pattern: "saveGeneratedDocument"
---

<objective>
Create test stubs for all Phase 14 requirements before implementation begins. This is a Wave 0 plan per the Nyquist Rule -- every verify command in Plans 01-03 needs a test file to run against.

Purpose: Ensures D-07 (Spanish prose diff testing) is addressed first, not as an afterthought. Provides test scaffolds that Plans 01-03 will make pass.
Output: 3 test files with failing test stubs covering AIGEN-V2-03 (version storage), AIGEN-V2-04 (diff computation with Spanish text), and AIGEN-V2-05 (revert logic).
</objective>

<execution_context>
@/Users/quantumcode/CODE/CARPETIFY/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/CODE/CARPETIFY/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-document-versioning/14-CONTEXT.md
@.planning/phases/14-document-versioning/14-RESEARCH.md
@.planning/phases/14-document-versioning/14-VALIDATION.md

<interfaces>
<!-- Existing test patterns to follow -->

From vitest.config.ts:
```typescript
// globals: true (describe, it, expect available globally)
// environment: jsdom (default), override with // @vitest-environment node
// alias: '@' -> /src, '@functions' -> functions/src
```

From src/__tests__/functions/stalenessTracker.test.ts (test pattern reference):
```typescript
// @vitest-environment node
import { describe, it, expect } from 'vitest';
import { ... } from '@functions/...';
// Tests use describe/it/expect pattern with descriptive test names
```

From functions/src/shared/types.ts:
```typescript
export type PassId = 'lineProducer' | 'financeAdvisor' | 'legal' | 'combined';
export type DocumentId = 'A1' | 'A2' | ... | 'PITCH';
export type VersionTriggerReason = 'regeneration' | 'manual_revert' | 'pipeline_run';
export interface DocumentVersion { ... }
```

From functions/src/pipeline/documentStore.ts:
```typescript
export async function saveGeneratedDocument(
  projectId: string,
  docId: DocumentId,
  content: unknown,
  passId: PassId,
  promptFile: string,
  modelUsed: string,
  triggeredBy?: string,
  triggerReason?: VersionTriggerReason,
): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create diffCompute test with Spanish prose test case (D-07)</name>
  <files>src/__tests__/versioning/diffCompute.test.ts</files>
  <action>
Create `src/__tests__/versioning/` directory and `diffCompute.test.ts`.

This test file directly addresses D-07: "the diff library MUST be tested with Spanish prose early in implementation -- Include a Spanish prose diff test case in the implementation plan, not as an afterthought."

**The test file must include:**

1. **Spanish prose diff test (D-07 critical):** Use `diffWords` from the `diff` library with real Spanish text containing accented characters. Test cases:
   - Basic Spanish text change: "La produccion cinematografica" vs "La produccion audiovisual" -- verify the change is detected at word level
   - Accented characters preserved: "El director artistico superviso la escenografia" vs "El director artistico rediseno la escenografia" -- verify accented chars (o with accent, o without) handled correctly
   - Long compound sentence: A multi-clause sentence typical of EFICINE prose with commas, accents (a, e, i, n with tilde, u with umlaut) -- verify diff doesn't split mid-word on accent boundaries
   - Empty string handling: diff between empty string and Spanish text

2. **JSON/structured diff test:** Use `diffJson` to compare two simple JSON objects (simulating budget data). Verify changes detected at field level.

3. **Each test should be a proper failing stub** -- import `diffWords` and `diffJson` from `diff`, write the assertion, but since the diff library is not yet installed (Plan 02 Task 1 installs it), these tests will fail with import error until then. That is the correct RED state.

**Pattern to follow** (from existing tests):
```typescript
import { describe, it, expect } from 'vitest';
import { diffWords, diffJson } from 'diff';

describe('diffCompute - Spanish prose (D-07)', () => {
  it('should detect word-level changes in Spanish text with standard characters', () => {
    const oldText = 'La produccion cinematografica requiere un presupuesto detallado';
    const newText = 'La produccion audiovisual requiere un presupuesto detallado';
    const changes = diffWords(oldText, newText);
    // ... assertions
  });

  it('should handle accented characters correctly: a, e, n with tilde, u with umlaut', () => {
    const oldText = 'El guion cinematografico fue escrito por Maria Gonzalez en la Ciudad de Mexico';
    const newText = 'El guion cinematografico fue revisado por Maria Gonzalez en la Ciudad de Mexico';
    const changes = diffWords(oldText, newText);
    // ... assertions that accented chars are NOT split
  });

  it('should handle long compound sentences typical of EFICINE documents', () => {
    // Real-world test: a sentence from a sinopsis-style document
    // ...
  });
});

describe('diffCompute - structured/JSON diff', () => {
  it('should detect field-level changes in budget-like JSON', () => {
    // ...
  });
});
```

**IMPORTANT per D-07:** The Spanish text MUST include these specific characters to prove Unicode handling:
- a with accent: a (U+00E1)
- e with accent: e (U+00E9)
- i with accent: i (U+00ED)
- o with accent: o (U+00F3)
- u with accent: u (U+00FA)
- n with tilde: n (U+00F1)
- u with umlaut: u (U+00FC)
- Inverted question mark: ? (U+00BF)
- Inverted exclamation: ! (U+00A1)
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/CARPETIFY && test -f src/__tests__/versioning/diffCompute.test.ts && grep -c "produccion\|producción" src/__tests__/versioning/diffCompute.test.ts</automated>
  </verify>
  <done>diffCompute.test.ts exists with at least 4 test cases, including Spanish prose with accented characters per D-07. Tests are valid stubs that will fail (RED) until diff library is installed in Plan 02.</done>
</task>

<task type="auto">
  <name>Task 2: Create documentStore version snapshot tests and revertDocument tests</name>
  <files>src/__tests__/functions/documentStore.test.ts, src/__tests__/functions/revertDocument.test.ts</files>
  <read_first>
    src/__tests__/functions/stalenessTracker.test.ts
    src/__tests__/functions/lineProducer.test.ts
    functions/src/pipeline/documentStore.ts
    functions/src/shared/types.ts
  </read_first>
  <action>
**1. Update or create `src/__tests__/functions/documentStore.test.ts`:**

If this file already exists, ADD a new describe block for version snapshot tests. If it does not exist, create it.

Test stubs for AIGEN-V2-03 (version storage):

```typescript
// @vitest-environment node
import { describe, it, expect } from 'vitest';

describe('saveGeneratedDocument - version snapshots (AIGEN-V2-03)', () => {
  it('should snapshot previous version to versions subcollection before overwrite', () => {
    // Stub: will be implemented when documentStore.ts is modified in Plan 01
    // Requires Firestore mock or integration test
    expect.fail('STUB: implement after Plan 01 modifies saveGeneratedDocument');
  });

  it('should include all required fields in version entry: content, contentType, version, passId, generatedAt, archivedAt, triggerReason, triggeredBy, modelUsed, promptFile', () => {
    expect.fail('STUB: implement after Plan 01 adds DocumentVersion type');
  });

  it('should delete oldest version when 10 versions exist (prune logic per D-04)', () => {
    expect.fail('STUB: implement after Plan 01 adds prune logic');
  });

  it('should use batched write for archive + prune atomicity (per D-04)', () => {
    expect.fail('STUB: implement after Plan 01 adds batch logic');
  });

  it('should handle first-time save with no existing document (no snapshot needed)', () => {
    expect.fail('STUB: implement after Plan 01');
  });

  it('should snapshot editedContent when present, falling back to content', () => {
    expect.fail('STUB: implement after Plan 01');
  });
});
```

**2. Create `src/__tests__/functions/revertDocument.test.ts`:**

Test stubs for AIGEN-V2-05 (revert):

```typescript
// @vitest-environment node
import { describe, it, expect } from 'vitest';

describe('revertDocumentVersion (AIGEN-V2-05)', () => {
  it('should copy target version content forward as new version N+1 (per D-09)', () => {
    expect.fail('STUB: implement after Plan 03 creates revertDocument.ts');
  });

  it('should set triggerReason to manual_revert on the new version', () => {
    expect.fail('STUB: implement after Plan 03');
  });

  it('should set triggeredBy to the authenticated user uid', () => {
    expect.fail('STUB: implement after Plan 03');
  });

  it('should throw not-found error if target version does not exist', () => {
    expect.fail('STUB: implement after Plan 03');
  });

  it('should throw invalid-argument error if docId is not in DOCUMENT_REGISTRY', () => {
    expect.fail('STUB: implement after Plan 03');
  });

  it('should preserve the original version in history (version N not deleted)', () => {
    expect.fail('STUB: implement after Plan 03');
  });
});
```

Follow the `// @vitest-environment node` pattern from existing functions tests. All tests should fail with `expect.fail()` providing a clear STUB message indicating which plan will make them pass.
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/CARPETIFY && test -f src/__tests__/functions/documentStore.test.ts && test -f src/__tests__/functions/revertDocument.test.ts && grep -c "STUB" src/__tests__/functions/revertDocument.test.ts</automated>
  </verify>
  <done>documentStore.test.ts contains version snapshot test stubs (6 tests covering AIGEN-V2-03). revertDocument.test.ts contains revert copy-forward test stubs (6 tests covering AIGEN-V2-05). All stubs fail with clear messages indicating which plan resolves them.</done>
</task>

</tasks>

<verification>
```bash
# All 3 test files exist
test -f src/__tests__/versioning/diffCompute.test.ts && echo "diffCompute: OK"
test -f src/__tests__/functions/documentStore.test.ts && echo "documentStore: OK"
test -f src/__tests__/functions/revertDocument.test.ts && echo "revertDocument: OK"

# Spanish text present in diff test (D-07)
grep "produccion\|producción\|cinematográfica\|guión" src/__tests__/versioning/diffCompute.test.ts

# Version snapshot stubs present
grep "snapshots previous version\|prune logic" src/__tests__/functions/documentStore.test.ts

# Revert stubs present
grep "copy.*forward\|manual_revert" src/__tests__/functions/revertDocument.test.ts
```
</verification>

<success_criteria>
1. `src/__tests__/versioning/diffCompute.test.ts` exists with Spanish prose test cases containing accented characters (D-07)
2. `src/__tests__/functions/documentStore.test.ts` has version snapshot and prune test stubs (AIGEN-V2-03)
3. `src/__tests__/functions/revertDocument.test.ts` has revert copy-forward test stubs (AIGEN-V2-05)
4. All tests are valid TypeScript that would run under vitest (but fail as stubs)
5. Test stubs clearly indicate which plan will make them pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-document-versioning/14-00-SUMMARY.md`
</output>
