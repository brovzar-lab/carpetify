---
phase: 16-milestone-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestore.rules
  - .planning/phases/12-realtime-collaboration/12-VERIFICATION.md
  - .planning/phases/11-rbac-access-control/11-VERIFICATION.md
autonomous: true
gap_closure: true
requirements:
  - COLLAB-04
  - COLLAB-01
  - COLLAB-02
  - COLLAB-03
  - COLLAB-05
  - COLLAB-07

must_haves:
  truths:
    - "userProjects/{userId}/projects/{projectId} Firestore writes succeed for authenticated self-access — activity badge resets to zero after viewing the activity tab"
    - "Phase 12 has a VERIFICATION.md with passed status confirming all 5 COLLAB requirements (01, 02, 03, 05, 07) are satisfied"
    - "Phase 11 VERIFICATION.md status updated from gaps_found to passed — stale TeamMembers gap resolved"
  artifacts:
    - path: "firestore.rules"
      provides: "userProjects self-access security rule"
      contains: "userProjects"
    - path: ".planning/phases/12-realtime-collaboration/12-VERIFICATION.md"
      provides: "Formal phase verification for real-time collaboration"
      contains: "status: passed"
    - path: ".planning/phases/11-rbac-access-control/11-VERIFICATION.md"
      provides: "Updated verification with resolved gap"
      contains: "status: passed"
  key_links:
    - from: "src/components/collaboration/ActivityTab.tsx"
      to: "firestore.rules"
      via: "setDoc on userProjects/{userId}/projects/{projectId}"
      pattern: "userProjects.*userId.*projects"
    - from: "src/components/wizard/WizardSidebar.tsx"
      to: "firestore.rules"
      via: "getDoc on userProjects/{userId}/projects/{projectId}"
      pattern: "userProjects.*userId.*projects"
---

<objective>
Close all three gaps identified in the v2.0 milestone audit: (1) add the missing userProjects Firestore security rule so activity badge resets work in production, (2) create the missing Phase 12 VERIFICATION.md to formally verify all real-time collaboration requirements, and (3) re-verify Phase 11 to update the stale gaps_found status now that TeamMembers is rendered.

Purpose: Bring all 6 pending COLLAB requirements to "satisfied" status and resolve all tech debt flagged in the milestone audit.
Output: Updated firestore.rules, new 12-VERIFICATION.md, updated 11-VERIFICATION.md.
</objective>

<execution_context>
@/Users/quantumcode/CODE/CARPETIFY/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/CODE/CARPETIFY/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/phases/12-realtime-collaboration/12-01-SUMMARY.md
@.planning/phases/12-realtime-collaboration/12-02-SUMMARY.md
@.planning/phases/12-realtime-collaboration/12-03-SUMMARY.md
@.planning/phases/11-rbac-access-control/11-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add userProjects Firestore security rule for self-access</name>
  <files>firestore.rules</files>
  <read_first>
    - firestore.rules (current rules — understand the structure and placement)
    - src/components/collaboration/ActivityTab.tsx (lines 87-94 — setDoc write pattern)
    - src/components/wizard/WizardSidebar.tsx (lines 62-78 — getDoc read pattern)
  </read_first>
  <action>
Add a new rule block to firestore.rules for the userProjects collection. This rule must allow authenticated users to read and write only their own data (self-access pattern: request.auth.uid == userId).

The exact rule to add, placed AFTER the existing `match /users/{userId}` block and BEFORE the legacy ERPI settings block:

```
// User-specific project metadata (e.g., lastViewedActivity for badge reset)
// Written by ActivityTab on mount, read by WizardSidebar for badge count
match /userProjects/{userId}/{document=**} {
  allow read, write: if isAuth() && request.auth.uid == userId;
}
```

This follows the exact pattern recommended in the milestone audit. The `{document=**}` wildcard covers the nested `projects/{projectId}` subcollection path used by both ActivityTab.tsx and WizardSidebar.tsx.

Do NOT modify any other rules. Do NOT change any existing rule blocks. Only ADD this new block.
  </action>
  <verify>
    <automated>grep -n "userProjects" firestore.rules | head -5</automated>
  </verify>
  <acceptance_criteria>
    - firestore.rules contains a `match /userProjects/{userId}/{document=**}` block
    - The rule checks `request.auth.uid == userId` for self-access
    - The rule allows both read and write operations
    - All existing rules remain unchanged
    - `npm run build` still passes (rules file is not part of the build, but verify no other files were accidentally modified)
  </acceptance_criteria>
  <done>userProjects security rule exists in firestore.rules allowing authenticated self-access — ActivityTab setDoc and WizardSidebar getDoc will succeed in production for the authenticated user's own path</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 12 VERIFICATION.md with formal goal-backward verification</name>
  <files>.planning/phases/12-realtime-collaboration/12-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/12-realtime-collaboration/12-01-SUMMARY.md (hooks, RTDB infrastructure)
    - .planning/phases/12-realtime-collaboration/12-02-SUMMARY.md (UI components)
    - .planning/phases/12-realtime-collaboration/12-03-SUMMARY.md (shell integration, human-verified)
    - .planning/ROADMAP.md (Phase 12 success criteria)
    - src/hooks/usePresence.ts (presence hook — verify exists and exports)
    - src/hooks/useProjectPresence.ts (presence reading — verify exists and exports)
    - src/hooks/useSectionLock.ts (locking hook — verify exists and exports)
    - src/hooks/useIdleDetection.ts (idle detection — verify exists and exports)
    - src/components/collaboration/PresenceAvatar.tsx (avatar component — verify exists)
    - src/components/collaboration/PresenceAvatarRow.tsx (avatar row — verify exists)
    - src/components/collaboration/LockBanner.tsx (lock UI — verify exists)
    - src/components/collaboration/ForceBreakDialog.tsx (force break — verify exists)
    - src/components/collaboration/SidebarPresenceDot.tsx (sidebar dots — verify exists)
    - src/components/wizard/WizardShell.tsx (verify presence/lock/idle hooks wired)
    - src/components/wizard/WizardSidebar.tsx (verify SidebarPresenceDot wired)
    - src/components/layout/AppHeader.tsx (verify PresenceAvatarRow wired)
    - src/hooks/useAutoSave.ts (verify lockOwned gating)
    - src/lib/rtdb.ts (RTDB path helpers — verify exists)
    - src/lib/firebase.ts (verify rtdb export)
    - functions/src/collaboration/forceBreakLock.ts (Cloud Function — verify exists)
    - database.rules.json (RTDB security rules — verify exists)
  </read_first>
  <action>
Create `.planning/phases/12-realtime-collaboration/12-VERIFICATION.md` following the exact format used in Phase 13's verification (see 13-VERIFICATION.md for reference structure).

The verification must check all 5 ROADMAP success criteria for Phase 12:

1. "Two users viewing the same project see each other's changes appear in real-time without manual refresh" (COLLAB-01)
   - Verify: usePresence writes to RTDB on mount, useProjectPresence subscribes via onValue, WizardShell passes presenceList to WizardSidebar, auto-save uses Firestore with onSnapshot (not polling)

2. "When one user is editing a wizard screen, other users see that screen marked as locked with the editor's name displayed" (COLLAB-02)
   - Verify: useSectionLock acquireLock writes to RTDB locks path, LockBanner renders with editor name from lock state, WizardShell conditionally shows LockBanner when lockHolder is present and not self

3. "Avatar indicators show which team members are currently in the project and which screen each is viewing" (COLLAB-03)
   - Verify: PresenceAvatarRow rendered in AppHeader, SidebarPresenceDot rendered per screen in WizardSidebar, useProjectPresence returns PresenceEntry[] with screen field

4. "A line_producer can edit the financial structure screen but cannot edit the creative team screen" (COLLAB-05)
   - Verify: WizardShell uses canEditScreen(role, activeScreen) to determine isReadOnly, ReadOnlyBanner shown when isReadOnly is true, edit button hidden when canEditScreen returns false

5. "A user attempting to edit a section already being edited by someone else sees 'en uso por [nombre]' conflict message" (COLLAB-07)
   - Verify: LockBanner uses es.collaboration.lock.banner locale string with editor name, ForceBreakDialog available for productor role, lock acquisition returns LockState with isLocked/lockHolder

For each truth, use grep to find the specific line numbers and file paths as evidence. Use the same frontmatter format as 11-VERIFICATION.md and 13-VERIFICATION.md.

Set status to `passed` — the 12-03 SUMMARY confirms human verification of all 7 collaboration features.

Include artifacts table covering all key files from all 3 plans. Include key links table showing the wiring from hooks to UI to shell. Include requirements coverage table for COLLAB-01, 02, 03, 05, 07.
  </action>
  <verify>
    <automated>grep -c "VERIFIED\|SATISFIED" .planning/phases/12-realtime-collaboration/12-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 12-VERIFICATION.md exists in .planning/phases/12-realtime-collaboration/
    - Frontmatter has status: passed
    - All 5 success criteria listed with VERIFIED status and specific file:line evidence
    - All key artifacts from plans 01, 02, 03 listed with verified status
    - Key links table shows wiring between hooks, components, and shell
    - Requirements coverage table covers COLLAB-01, 02, 03, 05, 07 all as SATISFIED
    - Evidence uses actual grep-confirmed line numbers, not estimates
  </acceptance_criteria>
  <done>Phase 12 VERIFICATION.md exists with passed status, all 5 success criteria verified with file-level evidence, all 5 COLLAB requirements marked SATISFIED</done>
</task>

<task type="auto">
  <name>Task 3: Re-verify Phase 11 VERIFICATION.md — update status from gaps_found to passed</name>
  <files>.planning/phases/11-rbac-access-control/11-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/11-rbac-access-control/11-VERIFICATION.md (current verification with gaps_found status)
    - src/components/wizard/WizardShell.tsx (lines 33 and 280-285 — confirm TeamMembers import and render)
    - src/components/project/TeamMembers.tsx (confirm component still exports correctly)
  </read_first>
  <action>
Update `.planning/phases/11-rbac-access-control/11-VERIFICATION.md` to reflect the resolved gap.

Specific changes:

1. **Frontmatter:** Change `status: gaps_found` to `status: passed`. Change `score: 2/3 success criteria verified` to `score: 3/3 success criteria verified`.

2. **Success Criterion 1 row:** Change status from "FAILED" to "VERIFIED". Update evidence to: "TeamMembers imported at WizardShell.tsx line 33 and rendered at line 283 with projectId, collaborators, ownerId props. InviteModal reachable via TeamMembers invite button. Productor can now access the full invitation flow from the datos screen."

3. **TeamMembers artifact:** Change status from "ORPHANED" to "VERIFIED". Update details to reference the WizardShell import/render lines.

4. **InviteModal artifact:** Change status from "ORPHANED via TeamMembers" to "VERIFIED". Update details to note reachability via TeamMembers.

5. **Key Link (TeamMembers -> wizard screen):** Change from "NOT_WIRED" to "WIRED". Update details: "Imported and rendered in WizardShell.tsx at line 33 (import) and line 283 (render) within the datos screen section."

6. **AUTH-05:** Change from "BLOCKED" to "SATISFIED". Update evidence to note TeamMembers is rendered, invite flow is now fully reachable.

7. **Anti-Patterns Found:** Clear the table or mark all as "RESOLVED".

8. **Gaps Summary:** Replace with a note that the gap has been resolved.

9. Add `re_verification: true` and `re_verified: 2026-03-27T00:00:00Z` to frontmatter. Add a "Re-verification" section at the top noting this is an update from the milestone audit.

Do NOT change any of the items that were already marked as VERIFIED or SATISFIED. Only update the items that were FAILED/ORPHANED/BLOCKED/NOT_WIRED.
  </action>
  <verify>
    <automated>grep -E "status:|score:|FAILED|ORPHANED|BLOCKED|NOT_WIRED" .planning/phases/11-rbac-access-control/11-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter status is "passed" (not "gaps_found")
    - Frontmatter score is "3/3 success criteria verified"
    - No remaining FAILED, ORPHANED, BLOCKED, or NOT_WIRED entries
    - Success criterion 1 shows VERIFIED with WizardShell.tsx evidence
    - AUTH-05 shows SATISFIED
    - re_verification field is true
    - All previously VERIFIED items remain unchanged
  </acceptance_criteria>
  <done>Phase 11 VERIFICATION.md updated from gaps_found to passed — all 3 success criteria verified, AUTH-05 satisfied, no remaining gaps or anti-patterns</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. Firestore rules: `grep "userProjects" firestore.rules` returns the new rule block
2. Phase 12 verification: `grep "status: passed" .planning/phases/12-realtime-collaboration/12-VERIFICATION.md` confirms passed status
3. Phase 11 re-verification: `grep "status: passed" .planning/phases/11-rbac-access-control/11-VERIFICATION.md` confirms updated status
4. No regressions: `npm run build` passes (frontend code unchanged except firestore.rules which is not compiled)
5. All 6 COLLAB requirements addressed: COLLAB-04 (rule fix), COLLAB-01/02/03/05/07 (Phase 12 verification)
</verification>

<success_criteria>
1. userProjects Firestore security rule allows authenticated self-access — grep confirms rule exists
2. Phase 12 VERIFICATION.md exists with passed status and all 5 success criteria verified
3. Phase 11 VERIFICATION.md updated to passed with 3/3 score — no remaining gaps
4. All 6 COLLAB requirements (01, 02, 03, 04, 05, 07) are formally verified or fixed
5. v2.0 milestone audit gaps are fully closed
</success_criteria>

<output>
After completion, create `.planning/phases/16-milestone-gap-closure/16-01-SUMMARY.md`
</output>
