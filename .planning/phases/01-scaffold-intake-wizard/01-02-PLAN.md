---
phase: 01-scaffold-intake-wizard
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/dashboard/DashboardPage.tsx
  - src/components/dashboard/ProjectCard.tsx
  - src/components/dashboard/PeriodGroup.tsx
  - src/components/dashboard/EmptyState.tsx
  - src/components/erpi/ERPISettingsPage.tsx
  - src/components/erpi/ERPICompanyForm.tsx
  - src/components/erpi/PriorProjectsList.tsx
  - src/components/layout/AppHeader.tsx
autonomous: true
requirements:
  - INTK-01
  - INTK-02
  - INTK-03
  - INTK-09
  - INTK-10

must_haves:
  truths:
    - "User sees 'Mis Proyectos' dashboard as home screen with period-grouped project cards"
    - "User can click '+ Nuevo Proyecto' and a blank project is created, navigating to the wizard"
    - "User can delete a project with a Spanish confirmation dialog"
    - "User can clone/duplicate a project"
    - "User can navigate to 'Datos ERPI' and enter shared company data"
    - "Projects are grouped under period headers per D-05"
  artifacts:
    - path: "src/components/dashboard/DashboardPage.tsx"
      provides: "Main dashboard with project cards grouped by period"
      min_lines: 50
    - path: "src/components/dashboard/ProjectCard.tsx"
      provides: "Project card showing title, genre, period, budget, readiness, days remaining"
      min_lines: 40
    - path: "src/components/erpi/ERPISettingsPage.tsx"
      provides: "ERPI shared settings page with company form and prior projects list"
      min_lines: 40
  key_links:
    - from: "src/components/dashboard/DashboardPage.tsx"
      to: "src/services/projects.ts"
      via: "listProjects(), createProject(), deleteProject(), cloneProject()"
      pattern: "createProject|deleteProject|cloneProject"
    - from: "src/components/dashboard/ProjectCard.tsx"
      to: "src/lib/format.ts"
      via: "formatMXN for budget display"
      pattern: "formatMXN"
    - from: "src/components/erpi/ERPISettingsPage.tsx"
      to: "src/services/erpi.ts"
      via: "getERPISettings(), updateERPISettings()"
      pattern: "getERPISettings|updateERPISettings"
---

<objective>
Build the project dashboard (home screen) with period-grouped project cards, quick create, delete, clone, and the ERPI shared settings page.

Purpose: This is the entry point of the app. Users manage projects from here and configure shared ERPI data that all projects reference. Per D-11, the dashboard is always the home screen.
Output: Functional dashboard with project CRUD, ERPI settings page with company data form and prior projects list.
</objective>

<execution_context>
@/Users/quantumcode/CODE/CARPETIFY/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/CODE/CARPETIFY/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scaffold-intake-wizard/01-CONTEXT.md
@.planning/phases/01-scaffold-intake-wizard/01-UI-SPEC.md
@.planning/phases/01-scaffold-intake-wizard/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan consumes -->

From src/services/projects.ts:
```typescript
export function createProject(): Promise<string>; // returns projectId
export function getProject(id: string): Promise<ProjectMetadata>;
export function updateProjectMetadata(id: string, data: Partial<ProjectMetadata>): Promise<void>;
export function deleteProject(id: string): Promise<void>;
export function cloneProject(id: string): Promise<string>; // returns new projectId
export function listProjects(): Promise<Array<ProjectMetadata & { id: string }>>;
```

From src/services/erpi.ts:
```typescript
export function getERPISettings(): Promise<ERPISettings>;
export function updateERPISettings(data: Partial<ERPISettings>): Promise<void>;
```

From src/schemas/project.ts:
```typescript
export const projectMetadataSchema: ZodObject;
export type ProjectMetadata;
```

From src/schemas/erpi.ts:
```typescript
export const erpiSettingsSchema: ZodObject;
export const proyectoPrevioSchema: ZodObject;
export type ERPISettings;
```

From src/lib/format.ts:
```typescript
export function formatMXN(centavos: number): string;
export function formatDateES(date: Date): string;
```

From src/locales/es.ts:
```typescript
export const es: { dashboard: {...}, erpi: {...}, ... };
```

From src/lib/constants.ts:
```typescript
export const PERIODOS_EFICINE: Record<string, { label: string; open: string; close: string }>;
```

From src/stores/appStore.ts:
```typescript
export const useAppStore: () => { activeProjectId: string | null; setActiveProject: (id: string | null) => void };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Dashboard page with project cards, period grouping, and CRUD operations</name>
  <files>
    src/components/dashboard/DashboardPage.tsx,
    src/components/dashboard/ProjectCard.tsx,
    src/components/dashboard/PeriodGroup.tsx,
    src/components/dashboard/EmptyState.tsx,
    src/components/layout/AppHeader.tsx
  </files>
  <read_first>
    src/services/projects.ts (CRUD function signatures),
    src/schemas/project.ts (ProjectMetadata type),
    src/lib/format.ts (formatMXN signature),
    src/lib/constants.ts (PERIODOS_EFICINE for period grouping),
    src/locales/es.ts (dashboard strings),
    .planning/phases/01-scaffold-intake-wizard/01-UI-SPEC.md (layout contract, copywriting contract, interaction contracts)
  </read_first>
  <action>
**A. DashboardPage.tsx** -- the home screen per D-11:

Layout per UI-SPEC Dashboard Layout:
- Max content width 1200px, centered
- Header row: Display-size title "Mis Proyectos" (28px/600 weight) left-aligned, "Datos ERPI" link (text button with settings icon, navigates to `/erpi`), "+ Nuevo Proyecto" primary CTA button right-aligned (per UI-SPEC focal point)
- Below header: projects grouped by period

Data loading:
- Use `@tanstack/react-query` useQuery to call `listProjects()` from services
- Show `<Skeleton>` cards while loading (3 skeleton cards)
- On empty: render `<EmptyState>`

Project CRUD wiring:
- "+ Nuevo Proyecto" button calls `createProject()` then navigates to `/project/{newId}/datos` (per D-08: quick create, lands in wizard, no modal)
- Delete: calls `deleteProject(id)` after confirmation dialog (per D-09)
- Clone: calls `cloneProject(id)`, shows toast `es.dashboard.cloneToast` ("Proyecto duplicado exitosamente") per UI-SPEC

Period grouping per D-05:
- Group projects by `periodo_registro` field
- Render `<PeriodGroup>` for each period with header text from `PERIODOS_EFICINE[period].label` (e.g., "Periodo 1 (Ene-Feb 2026)")
- Projects with no period go under "Sin periodo asignado"

**B. ProjectCard.tsx** per D-04:

Use shadcn `<Card>` component. Card content:
- Title (semibold 14px) + genre badge + period badge
- Completion % (placeholder: compute from filled fields later, show 0% for now)
- Budget: `formatMXN(costo_total_proyecto_centavos)` + "EFICINE:" `formatMXN(monto_solicitado_eficine_centavos)`
- Readiness score: show `es.dashboard.readyToSubmit` / `es.dashboard.missingDocs(n)` / `es.dashboard.blockers(n)` based on project state (default to blockers for now)
- Days until period closes: calculate from `PERIODOS_EFICINE[periodo].close` relative to current date, display as `es.dashboard.daysRemaining(n)`
- Action buttons (bottom of card): "Duplicar" (clone icon + tooltip per D-10), "Eliminar" (trash icon, destructive)
- Entire card is clickable, navigates to `/project/{id}/datos`

Delete confirmation dialog per D-09 and UI-SPEC:
- Title: "Eliminar proyecto"
- Body: `Se eliminara permanentemente "{titulo_proyecto}" y todos sus datos. Esta accion no se puede deshacer.`
- Confirm button: "Eliminar proyecto" (destructive variant, red)
- Cancel button: "Conservar proyecto" (outline variant)

**C. PeriodGroup.tsx:**
- Takes `period: string` and `projects: ProjectWithId[]`
- Renders period header text (20px/600 heading style)
- Renders responsive grid of ProjectCards: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4` (per UI-SPEC: 1 col < 768, 2 cols 768-1024, 3 cols > 1024)

**D. EmptyState.tsx:**
- Centered content per UI-SPEC:
- Heading: "Sin proyectos" (20px/600)
- Body: "Crea tu primer proyecto para comenzar a armar tu carpeta EFICINE." (14px/400)
- CTA button: "+ Crear Proyecto" (primary variant)
- Clean, minimal, professional per D-12

**E. AppHeader.tsx:**
- Minimal header with app name "Carpetify" (optional, subtle)
- Used as layout wrapper for the dashboard route
- Auto-save status indicator slot (for wizard pages, not used on dashboard)

**F. Update App.tsx routes:**
- Route `/` renders `<DashboardPage>`
- Wrap with TanStack QueryClientProvider in main.tsx or App.tsx
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/CARPETIFY && npx vitest run --reporter=verbose 2>&1 | tail -20 && grep -l "Mis Proyectos" src/components/dashboard/DashboardPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/dashboard/DashboardPage.tsx contains `Mis Proyectos` or imports from es.ts
    - src/components/dashboard/DashboardPage.tsx contains `createProject`
    - src/components/dashboard/DashboardPage.tsx contains `deleteProject`
    - src/components/dashboard/DashboardPage.tsx contains `cloneProject`
    - src/components/dashboard/ProjectCard.tsx contains `formatMXN`
    - src/components/dashboard/ProjectCard.tsx contains `Eliminar proyecto`
    - src/components/dashboard/PeriodGroup.tsx contains `grid`
    - src/components/dashboard/EmptyState.tsx contains `Sin proyectos`
    - src/components/dashboard/EmptyState.tsx contains `Crear Proyecto`
    - DashboardPage uses useQuery from @tanstack/react-query
    - Delete dialog uses shadcn Dialog component
    - Card navigates to `/project/` route on click
  </acceptance_criteria>
  <done>Dashboard renders at `/` with period-grouped project cards. Quick create navigates to wizard. Delete shows Spanish confirmation dialog. Clone duplicates project with toast. Empty state shows when no projects exist.</done>
</task>

<task type="auto">
  <name>Task 2: Build ERPI Settings page with company form and prior projects list</name>
  <files>
    src/components/erpi/ERPISettingsPage.tsx,
    src/components/erpi/ERPICompanyForm.tsx,
    src/components/erpi/PriorProjectsList.tsx
  </files>
  <read_first>
    src/schemas/erpi.ts (erpiSettingsSchema, proyectoPrevioSchema),
    src/services/erpi.ts (getERPISettings, updateERPISettings),
    src/hooks/useAutoSave.ts (auto-save hook),
    src/locales/es.ts (erpi section strings),
    .planning/phases/01-scaffold-intake-wizard/01-UI-SPEC.md (ERPI Settings layout and copywriting)
  </read_first>
  <action>
**A. ERPISettingsPage.tsx** per D-06 and UI-SPEC ERPI Settings Layout:

Layout:
- Full-width content area (no wizard sidebar), max 800px centered
- Back navigation link to dashboard: "< Mis Proyectos" (left arrow icon + text)
- Display-size title: "Datos ERPI" (28px/600)
- Description: "Informacion de la empresa solicitante. Se comparte entre todos los proyectos." (14px/400, muted color)
- Two sections below: company data form + prior projects list

Data loading:
- Use `@tanstack/react-query` useQuery to call `getERPISettings()`
- Use `useAutoSave` hook for debounced persistence (same as wizard screens per D-03)
- Show save status indicator ("Guardando..." / "Guardado") in top-right

**B. ERPICompanyForm.tsx** per D-06:

Use React Hook Form with `erpiSettingsSchema` resolver.

Form fields (all labels in Spanish from es.ts):
- Razon social (text input, required)
- RFC (text input, required, placeholder "XAXX010101000")
- Representante legal (text input, required)
- Domicilio fiscal (textarea, required)

Each field: shadcn `<Label>` + `<Input>`, 14px body text, md (16px) gap between fields.

On field change: trigger auto-save via `useAutoSave` hook debounce.

**C. PriorProjectsList.tsx** per D-06:

Section header: "Proyectos Previos EFICINE" (20px/600)

Dynamic list per D-15 pattern (similar to contributor list):
- Each row: titulo (text input), anio (number input), exhibido (switch toggle), estatus (select: exhibido/en_produccion/no_exhibido)
- "+ Agregar proyecto previo" button adds empty row
- X button removes row
- Auto-save on changes

This data feeds VALD-08 (ERPI eligibility: fewer than 2 unexhibited prior EFICINE projects) in Phase 4.

**D. Update App.tsx routes:**
- Route `/erpi` renders `<ERPISettingsPage>`
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/CARPETIFY && grep -l "Datos ERPI" src/components/erpi/ERPISettingsPage.tsx && grep -l "razon_social" src/components/erpi/ERPICompanyForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/erpi/ERPISettingsPage.tsx contains `Datos ERPI`
    - src/components/erpi/ERPISettingsPage.tsx contains link/button navigating back to dashboard
    - src/components/erpi/ERPICompanyForm.tsx contains `razon_social`
    - src/components/erpi/ERPICompanyForm.tsx contains `rfc`
    - src/components/erpi/ERPICompanyForm.tsx contains `representante_legal`
    - src/components/erpi/ERPICompanyForm.tsx contains `domicilio_fiscal`
    - src/components/erpi/ERPICompanyForm.tsx uses useForm from react-hook-form
    - src/components/erpi/PriorProjectsList.tsx contains `Agregar proyecto previo`
    - src/components/erpi/PriorProjectsList.tsx contains `exhibido`
    - ERPI page uses auto-save hook (useAutoSave)
    - Route `/erpi` is configured in App.tsx
  </acceptance_criteria>
  <done>ERPI settings page renders at `/erpi` with company form (razon social, RFC, representante legal, domicilio fiscal) and prior projects list. Auto-save persists changes. Back link returns to dashboard.</done>
</task>

</tasks>

<verification>
1. Navigate to `/` -- dashboard renders with "Mis Proyectos" title and "+ Nuevo Proyecto" button
2. Click "+ Nuevo Proyecto" -- new project created, navigates to wizard route
3. Project cards display title, genre, budget (formatted as $X,XXX,XXX MXN), period, readiness
4. Delete project shows confirmation dialog in Spanish, deletes on confirm
5. Clone project creates duplicate, shows toast
6. Navigate to `/erpi` -- ERPI settings page renders with company form and prior projects list
7. Empty state shows when no projects exist
</verification>

<success_criteria>
- Dashboard shows period-grouped cards with all D-04 data points
- Quick create works without modal per D-08
- Delete confirmation uses Spanish copy from UI-SPEC
- Clone duplicates project data per D-10
- ERPI settings page has company form + prior projects list per D-06
- All text is in Mexican Spanish per INTK-10
- All amounts displayed with formatMXN per LANG-02
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-intake-wizard/01-02-SUMMARY.md`
</output>
