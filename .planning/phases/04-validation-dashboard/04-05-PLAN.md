---
phase: 04-validation-dashboard
plan: 05
type: execute
wave: 3
depends_on: ["04-04"]
files_modified:
  - src/components/validation/ExpirationAlert.tsx
  - src/components/validation/ExpirationBadge.tsx
  - src/components/validation/HyperlinkVerifier.tsx
  - src/components/validation/ValidationProjectCardBadge.tsx
  - src/components/dashboard/ProjectCard.tsx
  - src/components/wizard/DocumentChecklist.tsx
  - src/components/wizard/TeamMemberForm.tsx
autonomous: false
requirements:
  - VALD-04
  - VALD-06
  - VALD-07
  - VALD-08
  - VALD-09
  - VALD-11
  - VALD-12
  - VALD-15
  - VALD-16
  - VALD-17

must_haves:
  truths:
    - "Project cards show clickable blocker/warning counts that navigate to validation dashboard"
    - "Document upload screen shows expiration badges next to affected documents"
    - "Expiration alert appears on project card when any document <= 14 days"
    - "Expired documents are strict blockers with no dismissal option"
    - "Hyperlink verification runs once on entry, caches result, has re-verify button"
    - "Ir al campo navigation highlights the target field for 3 seconds"
  artifacts:
    - path: "src/components/validation/ExpirationAlert.tsx"
      provides: "Reusable three-tier expiration alert component"
      exports: ["ExpirationAlert"]
    - path: "src/components/validation/ExpirationBadge.tsx"
      provides: "Compact days-remaining badge with color coding"
      exports: ["ExpirationBadge"]
    - path: "src/components/validation/HyperlinkVerifier.tsx"
      provides: "Inline link verification with cached results"
      exports: ["HyperlinkVerifier"]
    - path: "src/components/dashboard/ProjectCard.tsx"
      provides: "Updated project card with validation summary row"
  key_links:
    - from: "src/components/dashboard/ProjectCard.tsx"
      to: "src/hooks/useValidation.ts"
      via: "reads validation report for blocker/warning counts"
      pattern: "useValidation|blockers|warnings"
    - from: "src/components/wizard/DocumentChecklist.tsx"
      to: "src/components/validation/ExpirationBadge.tsx"
      via: "renders expiration badges next to expirable documents"
      pattern: "ExpirationBadge"
    - from: "src/components/wizard/TeamMemberForm.tsx"
      to: "src/components/validation/HyperlinkVerifier.tsx"
      via: "renders hyperlink verifier next to enlaces fields"
      pattern: "HyperlinkVerifier"
---

<objective>
Integrate validation touchpoints across the existing UI: project card badges, document expiration alerts at three touchpoints, hyperlink verification, and field-highlighting for "Ir al campo" navigation.

Purpose: Per D-04/D-15/D-16, validation data surfaces at three contextually appropriate places: the project card (which project needs attention?), the validation dashboard (what exactly is wrong?), and the relevant wizard screen (fix it here). This plan wires the validation engine output into the existing UI surfaces so validation information is impossible to miss.

Output: Expiration/hyperlink components + updates to ProjectCard, DocumentChecklist, and TeamMemberForm.
</objective>

<execution_context>
@/Users/quantumcode/CODE/CARPETIFY/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/CODE/CARPETIFY/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-dashboard/04-CONTEXT.md
@.planning/phases/04-validation-dashboard/04-UI-SPEC.md
@.planning/phases/04-validation-dashboard/04-04-SUMMARY.md

<interfaces>
From src/hooks/useValidation.ts (created by Plan 04):
```typescript
export function useValidation(projectId: string): {
  report: ValidationReport | null
  viabilityScore: ScoreCategory[]
  improvements: ImprovementSuggestion[]
  loading: boolean
}
```

From src/validation/types.ts:
```typescript
export interface ValidationReport {
  results: ValidationResult[]
  blockers: ValidationResult[]
  warnings: ValidationResult[]
  passed: ValidationResult[]
  skipped: ValidationResult[]
  canExport: boolean
  timestamp: Date
}
```

From src/validation/rules/documentExpiration.ts (created by Plan 02):
```typescript
// metadata includes per-doc expiration data:
// Record<string, { daysRemaining: number; status: 'vigente'|'proximo'|'critico'|'vencido' }>
```

From src/components/dashboard/ProjectCard.tsx:
```typescript
interface ProjectCardProps {
  id: string
  metadata: ProjectMetadata
  onDelete: (id: string) => void
  onClone: (id: string) => void
}
// Currently shows: es.dashboard.blockers(0) as placeholder text
// Needs: real blocker/warning counts from validation + expiration banner
```

From src/components/wizard/DocumentChecklist.tsx:
```typescript
// Renders list of required documents with upload status
// Currently shows: status (Subido/Faltante) per document
// Needs: ExpirationBadge for expirable documents
```

From src/components/wizard/TeamMemberForm.tsx:
```typescript
// Renders form per team member with enlaces array
// Currently: plain text input for URLs
// Needs: HyperlinkVerifier inline next to each URL
```

From src/lib/constants.ts:
```typescript
export const PERIODOS_EFICINE = {
  '2026-P1': { close: '2026-02-13' },
  '2026-P2': { close: '2026-07-15' },
} as const
```

From 04-UI-SPEC.md Interaction Contracts:
```
"Ir al campo" field highlight:
- ring-2 ring-primary/50 applied immediately
- Fades out via transition-opacity 3s ease after 3 seconds
- Target screen reads `highlight` query parameter

Document Expiration touchpoints:
1. Validation dashboard (VALD-17 rule detail)
2. Upload screen (Screen 5, inline next to each affected doc)
3. Project card banner (when any doc <= 14 days)

Hyperlink Verification:
- Cached per URL, "Verificar de nuevo" button
- CORS fallback: "No se pudo verificar automaticamente"

Project Card:
- Blocker count in red, clickable -> /project/{id}/validacion?filter=blockers
- Warning count in yellow, clickable -> /project/{id}/validacion?filter=warnings
- Expiration banner when any doc <= 14 days
```

From 04-UI-SPEC.md Copywriting Contract:
```
Expiration:
- Vigente — {n} dias restantes (green)
- Proximo a vencer — {n} dias restantes (yellow, <=30 days)
- Vence pronto — {n} dias restantes (red, <=14 days)
- Vencido — requiere reemplazo (red solid)
- Project card banner: {n} documento(s) vence(n) pronto
- Upload inline: Este documento vence en {n} dias. Sube una version actualizada antes del cierre de registro.
- Upload expired: Este documento esta vencido. Sube una version actualizada para continuar.

Hyperlink:
- Verificar enlace / Verificar de nuevo
- Enlace accesible / Enlace no accesible
- Verificando... / No se pudo verificar automaticamente
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create expiration and hyperlink components, update ProjectCard with validation data</name>
  <files>
    src/components/validation/ExpirationAlert.tsx
    src/components/validation/ExpirationBadge.tsx
    src/components/validation/HyperlinkVerifier.tsx
    src/components/validation/ValidationProjectCardBadge.tsx
    src/components/dashboard/ProjectCard.tsx
  </files>
  <read_first>
    src/components/dashboard/ProjectCard.tsx
    src/validation/rules/documentExpiration.ts
    src/validation/types.ts
    src/hooks/useValidation.ts
    src/locales/es.ts
    src/lib/constants.ts
    src/components/ui/badge.tsx
    src/components/ui/button.tsx
    src/components/ui/alert.tsx
    .planning/phases/04-validation-dashboard/04-UI-SPEC.md
    .planning/phases/04-validation-dashboard/04-CONTEXT.md
  </read_first>
  <action>
    **`ExpirationBadge.tsx`** — Compact badge for inline use:
    - Props: `{ daysRemaining: number; status: 'vigente' | 'proximo' | 'critico' | 'vencido' }`
    - 28px height badge with color-coded background:
      - vigente (>30 days): green bg, text: "Vigente — {n} dias restantes"
      - proximo (<=30 days): yellow bg, text: "Proximo a vencer — {n} dias restantes"
      - critico (<=14 days): red bg, text: "Vence pronto — {n} dias restantes"
      - vencido (<=0 days): solid red bg, text: "Vencido — requiere reemplazo"
    - Uses Badge component with appropriate variant
    - Text at 12px (text-xs)
    - All copy from es.validation.expiration.*

    **`ExpirationAlert.tsx`** — Larger alert for upload screen context:
    - Props: `{ docType: string; daysRemaining: number; status: 'vigente' | 'proximo' | 'critico' | 'vencido' }`
    - Uses Alert component from shadcn
    - For proximo/critico: yellow/red Alert with message "Este documento vence en {n} dias. Sube una version actualizada antes del cierre de registro."
    - For vencido: red Alert with "Este documento esta vencido. Sube una version actualizada para continuar."
    - For vigente: no alert (component returns null)

    **`HyperlinkVerifier.tsx`** — Inline URL verification:
    - Props: `{ url: string; onVerificationComplete?: (accessible: boolean) => void }`
    - State: { verified: boolean; accessible: boolean; checking: boolean; error: string | null }
    - On mount (or when url changes): attempts a client-side fetch with mode: 'no-cors' or HEAD request
    - If CORS blocks the request: show yellow text "No se pudo verificar automaticamente" with link to open URL in new tab
    - If accessible: green CheckCircle icon + "Enlace accesible"
    - If not accessible: red XCircle icon + "Enlace no accesible. Verifica que sea publico y no requiera contrasena."
    - "Verificar de nuevo" button (outline variant) appears after initial check
    - During checking: "Verificando..." spinner text
    - Caches result per URL in component local state (per D-12)
    - All copy from es.validation.hyperlink.*

    **`ValidationProjectCardBadge.tsx`** — Reusable badge row for project cards:
    - Props: `{ blockerCount: number; warningCount: number; projectId: string; expiringCount?: number }`
    - Renders clickable blocker count in red: "{n} bloqueador(es)" -> navigates to /project/{id}/validacion?filter=blockers
    - Renders clickable warning count in yellow: "{n} advertencia(s)" -> navigates to /project/{id}/validacion?filter=warnings
    - When blockerCount === 0: green text "Sin bloqueadores"
    - Uses react-router useNavigate for click handling
    - onClick must stop propagation to prevent the card's own click handler from firing

    **Update `src/components/dashboard/ProjectCard.tsx`:**
    - Import useValidation hook
    - Call `useValidation(id)` to get the report
    - Replace the placeholder `es.dashboard.blockers(0)` section with ValidationProjectCardBadge:
      ```tsx
      <ValidationProjectCardBadge
        blockerCount={report?.blockers.length ?? 0}
        warningCount={report?.warnings.length ?? 0}
        projectId={id}
      />
      ```
    - Add expiration banner below the validation row when any document has daysRemaining <= 14:
      ```tsx
      {expiringDocs > 0 && (
        <Alert variant="destructive" className="py-1 px-2">
          <AlertDescription className="text-xs">
            {es.validation.projectCardExpirationBanner(expiringDocs)}
          </AlertDescription>
        </Alert>
      )}
      ```
    - Extract expiringDocs count from the VALD-17 result in the validation report (look for ruleId 'VALD-17' in report.results, check metadata for docs with status 'critico' or 'vencido')
    - The validation report metadata for VALD-17 contains per-doc expiration data
    - Stop event propagation on the ValidationProjectCardBadge click area (already handled inside that component)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ExpirationBadge.tsx renders 4 visual states (vigente/proximo/critico/vencido) with correct colors
    - ExpirationBadge.tsx uses 12px text (text-xs) and 28px height
    - ExpirationAlert.tsx returns null for vigente status (no alert shown)
    - ExpirationAlert.tsx shows red Alert for vencido with "requiere reemplazo" text
    - HyperlinkVerifier.tsx does NOT import axios/node-fetch (uses browser fetch only)
    - HyperlinkVerifier.tsx has "Verificar de nuevo" button after initial check
    - HyperlinkVerifier.tsx handles CORS failure with yellow fallback text
    - ProjectCard.tsx imports useValidation and calls it with the project id
    - ProjectCard.tsx replaces es.dashboard.blockers(0) with ValidationProjectCardBadge
    - ProjectCard.tsx shows expiration banner when expiringDocs > 0
    - ValidationProjectCardBadge.tsx navigates to /project/{id}/validacion?filter=blockers on blocker click
    - All visible text sourced from es.validation.* or es.dashboard.* (no hardcoded English)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Expiration badges, hyperlink verifier, and project card validation integration all render with correct severity colors, clickable navigation to validation dashboard, and Spanish copy.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate expiration alerts into Screen 5 and hyperlink verification into Screen 3</name>
  <files>
    src/components/wizard/DocumentChecklist.tsx
    src/components/wizard/TeamMemberForm.tsx
  </files>
  <read_first>
    src/components/wizard/DocumentChecklist.tsx
    src/components/wizard/TeamMemberForm.tsx
    src/components/validation/ExpirationBadge.tsx
    src/components/validation/ExpirationAlert.tsx
    src/components/validation/HyperlinkVerifier.tsx
    src/validation/constants.ts
    src/schemas/documents.ts
    src/schemas/team.ts
    src/lib/constants.ts
    src/locales/es.ts
    .planning/phases/04-validation-dashboard/04-UI-SPEC.md
    .planning/phases/04-validation-dashboard/04-CONTEXT.md
  </read_first>
  <action>
    **Update `src/components/wizard/DocumentChecklist.tsx` (Screen 5):**

    Add expiration badges next to each uploaded document that is in the EXPIRABLE_DOC_TYPES list:
    1. Import ExpirationBadge and ExpirationAlert from '@/components/validation/'
    2. Import EXPIRABLE_DOC_TYPES from '@/validation/constants'
    3. Import PERIODOS_EFICINE from '@/lib/constants'
    4. Import differenceInCalendarDays and parseISO from 'date-fns'
    5. For each document row where `doc.tipo` is in EXPIRABLE_DOC_TYPES and `doc.fecha_emision` exists:
       - Compute daysRemaining = 90 - differenceInCalendarDays(closeDate, issueDate)
       - Determine status: vigente (>30), proximo (<=30), critico (<=14), vencido (<=0)
       - Render ExpirationBadge below the document status badge
       - For proximo/critico/vencido: also render ExpirationAlert below the row with contextual message
    6. The closeDate comes from the project's periodo_registro field via PERIODOS_EFICINE
    7. The project's periodo_registro must be passed as a prop or read from context

    Layout per UI-SPEC Document Row:
    ```
    +-- Document Row --------------------------------+
    | [upload icon] Cotizacion de Seguro   [Subido]  |
    |   [ExpirationBadge: Proximo a vencer — 22 dias]|
    |   [ExpirationAlert: Sube version actualizada]  |
    +------------------------------------------------+
    ```

    **Update `src/components/wizard/TeamMemberForm.tsx` (Screen 3):**

    Add HyperlinkVerifier next to each URL in the enlaces array:
    1. Import HyperlinkVerifier from '@/components/validation/'
    2. For each entry in the `enlaces` array field:
       - Render HyperlinkVerifier inline to the right of the URL text input
       - The HyperlinkVerifier shows verification status (green check / red X / yellow warning)
    3. Layout: URL input (flex-1) + HyperlinkVerifier (fixed width, ~120px for status text)

    **Field highlighting for "Ir al campo" navigation (D-03):**

    Both DocumentChecklist and TeamMemberForm need to read the `highlight` query parameter from the URL:
    1. Import `useSearchParams` from 'react-router'
    2. Read `highlight` query param
    3. If `highlight` matches a field ID (e.g., 'titulo_proyecto', 'honorarios'), apply a temporary highlight ring:
       - `ring-2 ring-primary/50` class on the target field
       - Use a useEffect that removes the ring after 3 seconds via setTimeout
       - Scroll the highlighted field into view using `scrollIntoView({ behavior: 'smooth', block: 'center' })`
    4. For TeamMemberForm: also read `member` query param to scroll to the specific team member form
    5. For DocumentChecklist: highlight the specific document row when navigating from a validation error

    NOTE: Field highlighting should also be added to ProjectSetup.tsx (Screen 1 for titulo_proyecto) and FinancialStructure.tsx (Screen 4 for financial fields), but keep those changes minimal:
    - Read `highlight` query param
    - Apply ring-2 ring-primary/50 to the matching field
    - Remove after 3s
    - These are the same 5 lines of code in each component
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - DocumentChecklist.tsx imports ExpirationBadge and EXPIRABLE_DOC_TYPES
    - DocumentChecklist.tsx renders ExpirationBadge for docs in EXPIRABLE_DOC_TYPES that have fecha_emision
    - DocumentChecklist.tsx computes daysRemaining using differenceInCalendarDays
    - DocumentChecklist.tsx shows ExpirationAlert for proximo/critico/vencido statuses
    - TeamMemberForm.tsx imports HyperlinkVerifier
    - TeamMemberForm.tsx renders HyperlinkVerifier next to each URL in enlaces array
    - DocumentChecklist.tsx reads useSearchParams for highlight param
    - TeamMemberForm.tsx reads useSearchParams for highlight and member params
    - Highlighted fields have ring-2 ring-primary/50 class that fades after 3 seconds
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Expiration badges appear inline on Screen 5 for expirable documents. Hyperlink verification appears on Screen 3 next to team member URLs. "Ir al campo" navigation highlights target fields for 3 seconds with smooth scroll.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete validation engine and dashboard</name>
  <files>n/a</files>
  <action>
    User verifies the complete Phase 4 validation system visually and functionally. All automation is complete -- this checkpoint confirms the integrated result works correctly in the browser.

    What was built across Plans 01-05:
    - 14 validation rules (10 blockers + 4 warnings) as pure TypeScript functions
    - Central engine orchestrator producing ValidationReport
    - Viability score estimation (deterministic, 38 pts) + AI persona artistic scoring (Cloud Function, 62 pts)
    - Validation dashboard page with two-panel layout, severity-grouped rules, score estimation panel
    - Real-time validation via useValidation hook with 300ms debounce
    - Project card validation badges with clickable navigation
    - Document expiration alerts at three touchpoints (dashboard, upload screen, project card)
    - Hyperlink verification with cached results
    - "Ir al campo" deep links from validation errors to exact wizard fields
    - All UI in Mexican Spanish
  </action>
  <verify>
    Steps for manual verification:
    1. Navigate to any project: click a project card on the dashboard
    2. Click "Validacion" in the sidebar -- verify the validation dashboard loads with two panels
    3. Verify rules are grouped by severity: Bloqueadores (red) at top, then Advertencias (yellow), then Cumplidas (green), then Sin evaluar (gray)
    4. Click any rule row to expand -- verify detail message, affected items list, and "Ir al campo" link appear
    5. Click "Ir al campo" on a rule -- verify it navigates to the correct wizard screen and highlights the target field with a brief ring animation
    6. Go back to the dashboard -- verify the project card shows blocker/warning counts (e.g., "3 bloqueadores", "2 advertencias")
    7. Click the blocker count on the project card -- verify it navigates to the validation dashboard with only Bloqueadores section expanded
    8. Navigate to Screen 5 (Documentos) -- verify expiration badges appear next to uploaded documents that have fecha_emision
    9. Navigate to Screen 3 (Equipo Creativo) -- verify hyperlink verification icons appear next to team member URLs
    10. In the validation dashboard, check the score estimation panel on the right -- verify viability scores show per-category progress bars
    11. Click "Evaluar puntaje" -- verify the AI evaluation runs and shows per-persona artistic scores (may take 15-30 seconds)
    12. Verify all text is in Mexican Spanish throughout
  </verify>
  <done>User confirms all validation dashboard features work correctly: rules display, severity grouping, score estimation, expiration alerts, hyperlink verification, "Ir al campo" navigation, and project card integration. All text in Spanish.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit && npx vitest run src/validation/ -x
</verification>

<success_criteria>
- Project cards show real validation data (blockers/warnings) with clickable navigation
- Expiration alerts surface at 3 touchpoints per D-16
- Hyperlink verification cached per URL with re-verify button per D-12
- "Ir al campo" highlights target field with ring-2 ring-primary/50 that fades after 3s
- All integration points use Spanish copy from locales
- Visual verification passes (checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-dashboard/04-05-SUMMARY.md`
</output>
